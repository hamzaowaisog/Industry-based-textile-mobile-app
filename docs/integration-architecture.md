# Integration Architecture — HamzaTex

**Type:** Backend API ↔ Mobile app (REST)  
**Parts:** backend (HamzaTex.Api), frontend (React Native / Expo)

---

## Overview

- **Frontend** calls **Backend** over HTTP/REST.
- **Backend** exposes `/api/*` endpoints; authentication via JWT (Bearer).
- **Frontend** uses Axios in `src/utils/api.js` with base URL (e.g. `http://localhost:5000/api`).

---

## Integration points

| From     | To      | Type     | Details                                                                                          |
| -------- | ------- | -------- | ------------------------------------------------------------------------------------------------ |
| frontend | backend | REST API | All API calls via `src/utils/api.js` (axios). Items CRUD, future: Login, Users, Clients, etc.    |
| frontend | backend | JWT      | Login returns JWT + refresh token; `Authorization: Bearer <token>` (to be wired in interceptor). |

---

## Data flow

1. **Login:** Frontend POSTs credentials to `/api/Login` → Backend returns JWT + refresh token.
2. **Authenticated requests:** Frontend sends `Authorization: Bearer <token>`; Backend validates via JWT middleware and policies (AdminOnly, StaffOnly, Authenticated).
3. **Refresh:** Frontend POSTs refresh token to `/api/Login/refresh` to get new access token.
4. **Business data:** Clients, Users, ClientTypes, etc. consumed by frontend when authenticated.

---

## Planned (from project direction)

- **Offline-first:** Firebase, SQLite on frontend; sync strategy TBD.
- **Enterprise / security:** Email verification, password reset, and related flows on backend.

---

_Generated by document-project workflow (deep scan)._
